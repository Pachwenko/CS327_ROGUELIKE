Extends the solution to 1.05 to supports teleportation both random and chosen by user.

All keys are mapped as outlines, and the selection when teleporting is displayed as an asterist, '*'.

All code is changed from C to C++ except for the heap. 
